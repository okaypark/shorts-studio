# 작업 지침

Codex는 먼저 [`CLAUDE.md`](./CLAUDE.md)와
[`docs/SHORTS-PRODUCTION-GUIDE.md`](./docs/SHORTS-PRODUCTION-GUIDE.md)를 모두 읽고 적용하세요.
충돌하면 쇼츠 제작 흐름·플랫폼·음성·자막·검증에는 최종 제작 지침을 우선하고,
저장소 구조와 컴포넌트 사용법에는 `CLAUDE.md`를 적용합니다.

사용자는 터미널을 직접 사용하지 않습니다. Codex가 필요한 검사·생성·렌더 명령을 실행하고,
초보 파트너에게는 현재 단계의 선택지를 쉬운 말로 적극 안내하세요. 한 번에 질문을 쏟지 말고
결과에 영향을 주는 질문을 2~4개씩 하며, 사용자가 “알아서 해줘”라고 하면 추천 기본값으로
계속 진행하세요.

핵심만 옮기면:

- 이 저장소는 템플릿이 아니라 **부품 창고**입니다. 영상마다 `videos/<slug>/Video.tsx` 에서 자유롭게 짭니다
- 창고는 `src/library/` 94종. 목록은 `docs/CATALOG.md`
- clippkit 13종은 props 를 받아 그대로 import, RVE 81종은 영상 폴더로 복사해서 수정
- 한글에는 반드시 `korean` 스타일을 펼쳐 넣습니다 (안 하면 단어 중간에서 줄이 끊깁니다)
- 직전 영상과 같은 얼굴이 나오면 실패입니다. 컨셉을 매번 새로 잡으세요
- 건강·금융 주제는 `docs/COMPLIANCE.md` 를 먼저 확인
- 음성은 대본 확정 직후 먼저 생성하고 실제 MP3 길이로 씬·자막 타이밍을 정합니다 — `docs/VOICE.md`
- 자막은 하단 고정이 아니며 플랫폼 UI와 이미지·얼굴·아이콘을 피해 자유롭게 배치합니다
- TikTok·Reels·TV 예능형 자막을 주제에 맞게 선택하되 가독성과 한글 의미 단위 줄바꿈을 우선합니다
- 미리보기 `npm run studio`, 출력 `npm run make <슬러그> out/<이름>.mp4`
