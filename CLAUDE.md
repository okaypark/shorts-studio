# 이 저장소에서 영상을 만드는 방법

이 문서는 사람이 아니라 **AI가 읽는 작업 지침**입니다.
사용자는 터미널을 직접 쓰지 않습니다. "이런 영상 만들어줘"라고 하면 아래대로 하세요.

쇼츠를 만들기 전에 반드시 [`docs/SHORTS-PRODUCTION-GUIDE.md`](./docs/SHORTS-PRODUCTION-GUIDE.md)를
함께 읽으세요. 플랫폼 선택, 파트너 안내 질문, 음성 우선 제작, 자유 자막 배치, TikTok·예능형
모션 자막, 이미지·아이콘 겹침 방지와 렌더 검증은 그 문서가 최종 기준입니다.
디자인이 정해지지 않았으면 주제에 맞는 서로 다른 비주얼 콘셉트 3개와 추천안을 먼저 제안하세요.
현재 환경에 이미지 생성 도구가 없으면 생성한 척하지 말고 씬별 이미지 프롬프트와 에셋 계획을 제공합니다.

---

## 이 저장소의 성격

**틀에 내용을 채우는 곳이 아니라, 부품 창고입니다.**

정해진 템플릿이 없습니다. 영상마다 자기 폴더에서 자유롭게 짭니다.
`src/library/` 에 컴포넌트 94종이 있으니 필요한 걸 골라 쓰고, 없으면 직접 쓰세요.

이게 의도된 설계입니다. 사용자가 여럿이고, **각자 다른 컨셉의 영상이 나와야 합니다.**
그래서 "이 저장소의 스타일"이라는 게 없습니다. 매번 이 주제에 맞는 화면을 새로 만드세요.

지켜야 하는 건 딱 하나, 아래 한글 규칙뿐입니다.

---

## 유일한 강제 규칙 — 한글 타이포

```tsx
import { korean } from "../../src/library/studio";

<h1 style={{ ...korean, fontSize: 90, color: "#fff" }}>
  {"실내에만 있으면\n비타민D는 안 만들어집니다"}
</h1>
```

`korean` 을 안 넣으면 "비타민D는"이 "비타 / 민D는"으로 끊깁니다. 예외 없이 넣으세요.
창고 컴포넌트는 영어 기준이라 한글을 모릅니다 — **감싸는 상자에 `korean` 을 펼쳐 넣으면 상속됩니다.**

그리고 화면 아래위는 플랫폼 UI에 가립니다. `SAFE_AREA` 안쪽에 중요한 글자를 두세요.

---

## 작업 절차

### 1. 주제를 받으면

플랫폼, 시청자, 목적, 길이가 결과를 바꾸는데 정보가 없다면 쉬운 선택지로 질문하세요.
한 번에 2~4개만 묻고, 이미 충분히 설명했거나 “알아서 해줘”라고 하면 추천값으로 진행하세요.

**컨셉을 먼저 정하세요.** 색, 정렬, 모션, 전환을 이 주제에 맞게 새로 고릅니다.
직전에 만든 영상과 같은 얼굴이 나오면 실패입니다. 두 예시 영상
(`videos/vitamin-d-morning`, `videos/desk-stretch-3`)을 나란히 보면 어느 정도로 달라야 하는지 알 수 있습니다.

### 2. 대본을 씁니다

`docs/SCRIPT-GUIDE.md` 의 규칙을 따릅니다. 요약하면:

- 첫 3초는 질문이나 반전. "안녕하세요", "오늘은 ~에 대해"로 시작하지 않습니다
- 한 장면에 한 가지 생각
- 자막은 내레이션 문장 그대로. 화면 큰 글씨와는 **다른 말**이어야 합니다
- 전체 20초 안팎

대본을 `videos/<slug>/script.md` 에 남겨 두면 나중에 고칠 때 편합니다.

### 3. 창고에서 고릅니다

`docs/CATALOG.md` 에 94종 목록이 있습니다. 쓰는 법이 두 가지입니다.

- `src/library/clippkit/` 13종 — props 를 받습니다. **그대로 import** 해서 값만 넘기세요
- `src/library/rve/` 81종 — 내용이 안에 박혀 있습니다. **영상 폴더로 복사**해서 고치세요

원본은 고치지 마세요. 창고를 깨끗이 둬야 `npm run update` 로 갱신해도 작업이 안 날아갑니다.

`src/library/studio/` 에는 이 저장소에서 만든 기본기 세 개가 있습니다 —
`Backdrop`(배경+스크림+켄번스), `Subtitle`(하단 자막), `korean`(타이포 규칙).
편의를 위한 것이라 안 써도 됩니다. 배경을 직접 그리고 싶으면 그렇게 하세요.

### 4. 영상 폴더를 만듭니다

```
videos/<slug>/
  Video.tsx      ← 자유롭게 짠 컴포지션. export 이름은 파스칼케이스
  script.md      ← 대본 (선택)
```

그리고 `videos/index.ts` 에 등록합니다:

```ts
defineVideo({ id: "slug", title: "제목", component: MyVideo, seconds: 20 })
```

`npm run new -- <slug>` 로 뼈대를 만들 수도 있습니다.

### 5. 음성이 필요하면 (선택)

내레이션을 넣으면 **화면 길이를 손으로 맞출 필요가 없어집니다.**

1. `videos/<슬러그>/narration.ts` 에 장면별 문장을 적습니다 (대본의 자막 문장 그대로)
2. `npm run voice -- <슬러그>` — 기본 Edge TTS로 MP3를 먼저 생성합니다
3. 장면 길이를 `frames("hook")` 처럼 음성에서 가져오고, 장면 안에 `<Narration take={take("hook")} />` 를 얹습니다

자세한 건 `docs/VOICE.md`. 내 목소리는 Edge TTS 결과에 OpenVoice V2 음색을 적용하고,
오프라인이면 MeloTTS를 사용합니다. ElevenLabs는 API 키와 비용 승인이 있을 때만 사용합니다.
실제 MP3 길이를 측정한 뒤 씬 길이와 자막 타이밍을 정하세요.

음성이 필요 없는 영상도 많습니다. 그때는 장면 길이를 직접 적으면 됩니다
(`videos/desk-stretch-3` 이 그 예).

### 6. 확인시킵니다

```
npm run studio
```

브라우저가 열립니다. "왼쪽 목록에서 방금 만든 영상을 눌러 보세요"라고 안내하세요.

### 7. 파일로 뽑습니다

```
npm run make <슬러그> out/<이름>.mp4
```

---

## 배경 이미지

`public/images/backgrounds/` 에 12종이 있습니다. 전부 이 저장소에서 코드로 만든 것이라
어떤 주제에 써도 라이선스 문제가 없습니다.

`dawn-green` `soft-mint` `calm-teal` `deep-forest` `slate-blue` `dusk-violet`
`graphite` `sand-warm` `amber-dusk` `clay-rose` `ocean-night` `paper-light`

배경을 아예 안 써도 됩니다. `grid-pulse`, `bokeh-circles`, `gradient-shift`, `starfield` 같은
창고의 배경 컴포넌트를 쓰거나, CSS 그라디언트를 직접 그리는 쪽이 나을 때도 많습니다.

사진이 필요하면 사용자에게 `public/images/` 에 넣어 달라고 하세요.
**웹에서 찾은 이미지를 임의로 내려받아 저장소에 넣지 마세요** — 이유는 `LICENSE-NOTICE.md`.

---

## 하지 말아야 할 것

- 직전 영상과 같은 색·같은 정렬·같은 전환을 그대로 쓰는 것 — 다양성이 이 저장소의 목적입니다
- `src/library/` 원본 파일을 고치는 것 — 사본을 영상 폴더에 두세요
- `korean` 없이 한글을 찍는 것
- 자막 없이 화면 글자만 넣는 것 — 소리 끄고 보는 사람이 다수입니다
- 효능·효과를 단정하는 표현 — `docs/COMPLIANCE.md` 확인
- 출처 없는 인용이나 통계

---

## 문제가 생기면

**`npm run studio` 가 안 열림**
→ Node.js 20 이상이 필요합니다 (`node -v`). 없으면 nodejs.org에서 LTS 설치.

**설치 실패 / 경로 오류**
→ 저장소가 `C:\Users\<사용자>\Documents\shorts-studio`에 있는지 확인. Windows는 경로가 길거나
한글·공백이 섞이면 `node_modules` 깊은 곳에서 실패합니다.

**렌더가 한참 멈춤**
→ 첫 렌더 때 Remotion이 브라우저 엔진을 내려받습니다. 몇 분 걸립니다.

**창고 컴포넌트가 화면 한가운데 박혀서 안 움직임**
→ 대부분 `position: absolute; top: 50%; left: 50%` 로 되어 있습니다.
크기를 가진 상자로 감싸면 원하는 자리에 들어옵니다.

**최신 창고를 받고 싶음**
→ `npm run update`. 저장 안 된 변경이 있으면 멈추고 알려줍니다.
